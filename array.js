var arr = ['kl.txt', 'M4', 'hello.txt', 'N7'];

var files = [];
var folders = [];

arr.forEach(function(val) {
    if (indexOf(arr) === -1);
    console.log('folder');
    else console.log('files');
})